interface:
  display_name: "Delta UniApp 小程序"
  short_description: "Shopro Delta 页面、App API、身份与分页工作流"
  default_prompt: "只修改 UniApp Delta 前端，先核对真实 App Controller；没有 App API 时停止真实接入。"
policy:
  allow_implicit_invocation: true
