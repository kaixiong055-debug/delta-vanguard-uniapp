你现在负责“Delta 先锋俱乐部”芋道商城改造项目的 **UniApp 微信小程序端第二阶段开发**。

本次只修改当前 Shopro/UniApp 小程序仓库，不修改 Java 后端，不修改 Vue 管理后台。

不要只给方案或代码片段，请先检查真实仓库，然后直接完成代码修改、构建验证和结果汇报。

# 一、项目边界

当前项目基于芋道商城 Shopro/UniApp。

必须遵守以下边界：

1. 商城首页、个人中心和商城底部导航继续由后台 DIY 动态装修。
2. 不修改 DIY JSON 协议。
3. 不修改 DIY 装修渲染器。
4. 不修改商城动态底部导航逻辑。
5. 不重写微信登录、商品、SKU、购物车、支付和原商城订单。
6. 打手端使用独立固定页面和 `pages-worker` 分包。
7. 老板服务履约页面使用 `pages-delta` 分包。
8. 老板和打手共用现有 `member_user` 和现有微信登录流程。
9. 打手身份、申请和资料接口统一使用 `/delta/**` 对应的 App API。
10. 商城交易订单和 Delta 服务履约订单必须保持分离。
11. 不修改 Java 后端和 Vue 管理后台文件。
12. 本阶段完成后停止，不开发接单大厅、任务进度、收入、提现和指定打手。

# 二、第一阶段现状

第一阶段已经可能存在以下代码：

* `pages-worker` 分包
* `pages-delta` 分包
* Delta API 基础文件
* `sheep/store/delta.js`
* 打手端基础页面
* 打手自定义底部菜单基础组件

这些文件可能已经生成并且可能存在未提交修改。

必须先检查真实代码，禁止重复创建已有文件，禁止直接覆盖现有实现。

# 三、开始前必须执行的检查

先执行并记录：

```bash
git status --short
git branch --show-current
```

然后检查：

```text
pages.json
package.json
pages/index/user.vue
sheep/api/
sheep/store/
sheep/router/
pages-worker/
pages-delta/
```

重点搜索：

```text
delta
worker
identity
application
app_mode
delta_app_mode
request
http
baseURL
/app-api
s-layout
s-block-item
showAuthModal
login
upload
```

必须确认：

1. `pages-worker` 和 `pages-delta` 是否已经注册。
2. 第一阶段页面、组件、API 和 Store 的真实路径。
3. 项目现有 request 封装方式。
4. API 文件是否只需要写 `/delta/**`。
5. request 是否已经自动添加 `/app-api`。
6. 当前登录判断和唤起登录的标准方式。
7. 页面跳转使用的项目现有 router 方法。
8. 项目现有上传组件和上传返回值格式。
9. 项目真实存在的微信小程序构建命令。
10. 当前仓库是否有未提交代码。

禁止执行：

```bash
git reset --hard
git checkout .
git clean -fd
git restore .
```

不得覆盖、回滚或删除用户已有未提交代码。

# 四、后端接口核验要求

候选接口如下：

```text
GET  /delta/worker/identity
POST /delta/worker/apply
GET  /delta/worker/application
GET  /delta/worker/profile
PUT  /delta/worker/profile
PUT  /delta/worker/work-status
```

先在当前仓库中搜索接口文档、已有 API 文件、类型定义、调用示例和字段名称。

如果当前工作区同时包含 Java 后端仓库，可以只读取后端 Controller、VO、DTO 和枚举进行核验，但不得修改任何后端文件。

重点核验：

* Controller 实际路径
* 是否由 request 自动拼接 `/app-api`
* 请求方法
* 请求参数
* 返回字段
* 状态枚举值
* 驳回原因字段
* 停用或拉黑字段
* 申请表单字段
* 图片字段是字符串、数组还是对象
* 工作状态字段和值

严禁出现：

```text
/app-api/app-api/delta/**
```

如果 request 已自动拼接 `/app-api`，API 文件必须使用：

```text
/delta/worker/**
```

如果无法访问后端源码，且当前仓库没有可靠接口字段定义：

1. 不写 Mock。
2. 不编造字段。
3. 优先复用已经存在的 Delta API 和类型。
4. 对无法确认的字段做最小、集中式兼容处理。
5. 在最终报告中明确列出“未能核验的字段”。
6. 不得为了页面能显示而硬编码虚假身份或申请状态。

# 五、本阶段实现范围

本阶段只实现以下功能：

1. 商城“我的”页面固定打手入口。
2. 打手身份查询。
3. 打手申请页面。
4. 申请状态页面。
5. 审核中、已驳回、已通过、已停用或拉黑状态处理。
6. 老板模式和打手模式切换。
7. 审核通过后进入打手工作台基础页。
8. 从打手端返回商城模式。

# 六、身份与状态原则

打手身份必须以后端实时查询结果为准。

本地存储：

```text
delta_app_mode
```

只能保存当前界面模式，例如：

```text
shop
worker
```

它不能作为打手权限依据。

每次进入打手入口或打手工作台时，都必须重新通过后端接口核验身份。

不得仅因为本地模式是 `worker` 就允许进入打手端。

建议统一处理以下业务状态，但必须以真实后端字段和枚举为准：

```text
未申请
审核中
已驳回
已通过
已停用
已拉黑
```

可能存在数字枚举或字符串枚举，必须根据真实代码适配，不要擅自定义与后端冲突的新枚举。

需要实现统一的状态归一化方法，避免状态判断散落在多个页面。

# 七、“我的”页面固定入口

修改：

```text
pages/index/user.vue
```

要求：

1. 找到页面中的 DIY 组件循环。
2. 固定打手入口必须放在 DIY 组件循环之后。
3. 入口必须仍然位于 `s-layout` 内。
4. 不修改 `s-block-item`。
5. 不修改 `s-layout`。
6. 不修改 DIY API。
7. 不改变原商城个人中心装修内容。
8. 不改变原商城动态底部导航。
9. 使用项目现有卡片、间距、图标和颜色规范。
10. 入口文案可根据身份显示不同状态。

入口建议展示：

```text
打手中心
申请成为打手 / 进入打手工作台 / 查看审核状态
```

点击入口后的流程：

```text
未登录
  -> 调用项目现有登录流程
  -> 登录成功后重新查询身份

未申请
  -> 进入打手申请页

审核中
  -> 进入申请状态页

已驳回
  -> 进入状态页
  -> 显示驳回原因
  -> 允许重新提交申请

已通过
  -> 设置 delta_app_mode = worker
  -> 进入打手工作台基础页

已停用或拉黑
  -> 禁止进入打手工作台
  -> 显示明确提示
```

不要在 `pages/index/user.vue` 内堆积全部身份逻辑。

身份查询、状态判断和模式切换应优先封装到现有 `sheep/store/delta.js`、Delta API 或独立工具文件中。

# 八、登录处理

未登录时必须调用项目现有登录流程。

禁止：

* 自己重新实现微信登录
* 自己写登录弹窗
* 自己保存伪造用户信息
* 跳转到不存在的登录页面
* 直接提示“请先登录”后结束

必须搜索项目现有登录调用方式，例如：

```text
showAuthModal
isLogin
login
auth
sheep.$router
```

按照项目现有方式唤起登录。

登录成功后，应重新执行打手身份查询，不要要求用户再次点击入口。

# 九、Delta Store 改造

检查并复用：

```text
sheep/store/delta.js
```

根据真实代码补充或整理以下能力：

```text
identity
application
profile
loading
identityLoaded
currentMode
```

以及对应方法：

```text
fetchWorkerIdentity
fetchWorkerApplication
fetchWorkerProfile
setAppMode
enterWorkerMode
exitWorkerMode
clearWorkerState
resolveWorkerRoute
```

实际命名可以遵循现有项目风格，不强制完全一致。

要求：

1. 身份查询结果保存在 Store 中。
2. 进入打手端前支持强制刷新身份。
3. 防止短时间内重复并发请求。
4. 请求失败时不要自动把用户当成未申请。
5. 权限判断不能只依赖缓存。
6. 退出登录时应能清理 Delta 身份缓存。
7. `delta_app_mode` 仅保存界面模式。
8. Store 不存储虚假身份状态。

# 十、API 文件

检查第一阶段已有 Delta API 文件，不要重复创建同功能文件。

根据项目现有 API 风格实现或补齐：

```javascript
getWorkerIdentity()
applyWorker(data)
getWorkerApplication()
getWorkerProfile()
updateWorkerProfile(data)
updateWorkerWorkStatus(data)
```

只实现后端真实存在的接口。

要求：

1. 使用项目现有 request 实例。
2. 不直接引入新的 Axios。
3. 不自行创建第二套 request。
4. API 路径是否包含 `/app-api` 以项目现有封装为准。
5. 请求方法必须与后端一致。
6. 请求体格式必须与后端一致。
7. 不吞掉后端错误信息。
8. 不写 Mock 数据。
9. 不编造成功结果。

# 十一、打手申请页面

检查第一阶段是否已有申请页面，优先在原页面上补充，不重复创建。

申请表单字段必须以后端真实 ApplyReqVO 为准。

可能涉及但不能直接假设存在的字段：

```text
真实姓名
手机号
微信号
游戏昵称
游戏 UID
游戏端
擅长地图
擅长服务
个人介绍
实力证明图片
查挂证明图片
在线时间
备注
```

只展示和提交后端真实接收的字段。

表单要求：

1. 使用 Vue 3 和 `script setup`。
2. 使用项目现有表单组件。
3. 使用项目现有上传组件。
4. 使用项目现有样式规范。
5. 有明确必填校验。
6. 手机号等字段按真实要求校验。
7. 图片数量和格式按后端字段限制。
8. 防止重复提交。
9. 提交中显示 loading。
10. 提交成功后刷新身份和申请状态。
11. 提交成功后进入申请状态页。
12. 提交失败显示后端真实错误。
13. 已驳回重新申请时，尽量回填原申请资料。
14. 不允许已通过用户重复提交申请。
15. 停用或拉黑用户不得重新申请，除非后端明确允许。

# 十二、申请状态页面

检查第一阶段是否已有状态页，优先复用。

页面需要支持：

## 1. 审核中

展示：

```text
申请已提交
平台正在审核
请耐心等待
```

可以提供刷新按钮。

不得显示虚假的预计审核时间。

## 2. 已驳回

展示：

```text
审核未通过
驳回原因
重新申请按钮
```

驳回原因必须取后端真实字段。

重新申请时：

* 进入申请页
* 尽量回填已有申请内容
* 不清空可复用资料

## 3. 已通过

展示：

```text
审核已通过
进入打手工作台
```

点击后：

* 再次核验身份
* 设置界面模式为 worker
* 进入工作台

## 4. 已停用或拉黑

展示明确的不可进入状态，例如：

```text
当前打手账号已停用
当前账号暂不可进入打手端
```

如果后端有原因字段则展示。

不得展示进入工作台按钮。

## 5. 未申请

状态页检测到未申请时，应跳转申请页或显示申请按钮。

# 十三、模式切换

模式只表示当前 UI 模式：

```text
shop
worker
```

进入打手工作台：

```text
后端身份核验通过
-> 保存 delta_app_mode = worker
-> 跳转打手工作台
```

返回商城模式：

```text
保存 delta_app_mode = shop
-> 回到商城首页或商城“我的”页面
```

要求：

1. 不修改商城动态 tabBar。
2. 打手端继续使用自定义固定底部菜单。
3. 返回商城后恢复商城原页面和原动态底部导航。
4. 不通过清空登录状态实现模式切换。
5. 不创建第二套用户体系。
6. 不通过 `delta_app_mode` 绕过后端身份校验。
7. 已停用或拉黑时，即使本地保存了 worker 模式，也必须强制返回商城模式。

# 十四、打手工作台基础页

审核通过后进入已有或新建的打手工作台基础页面。

只做基础工作台框架，不实现业务大厅。

允许展示：

```text
用户头像和昵称
打手审核状态
当前工作状态
基础欢迎信息
后续功能占位说明
返回商城模式按钮
```

禁止实现：

```text
接单大厅
抢单
任务列表
任务详情
任务进度
完成凭证
收入
余额
提现
排行榜
指定打手
```

如果第一阶段已经有工作台页面，只补充身份守卫、模式切换和返回商城，不重写整个页面。

工作台页面进入时：

1. 强制请求后端身份。
2. 未登录则调用现有登录流程或返回商城。
3. 未申请进入申请页。
4. 审核中进入状态页。
5. 已驳回进入状态页。
6. 已通过允许停留。
7. 停用或拉黑强制返回商城模式并提示。

# 十五、页面路由与分包

检查 `pages.json` 中已有路由。

不得重复注册相同页面。

推荐页面职责，但以现有文件为准：

```text
pages-worker/index/index.vue
pages-worker/apply/index.vue
pages-worker/application/index.vue
```

或者项目第一阶段已经使用的对应路径。

要求：

1. 所有打手固定页面放在 `pages-worker`。
2. 老板履约页面继续放在 `pages-delta`。
3. 本阶段不新增与需求无关的页面。
4. 页面跳转使用项目现有 router。
5. 避免直接散落大量 `uni.navigateTo`，除非项目原本就是这种风格。
6. 页面路径统一集中维护，减少硬编码。

# 十六、错误处理

需要区分：

```text
接口请求失败
身份不存在
未申请
审核中
驳回
通过
停用
拉黑
登录失效
```

接口请求失败时：

* 显示请求失败提示
* 提供重试
* 不自动跳申请页
* 不把失败当成未申请
* 不修改本地权限状态

后端返回登录失效时，按照项目现有拦截器和登录流程处理。

# 十七、代码质量要求

1. Vue 3。
2. 使用 `script setup`。
3. 遵循项目现有代码格式。
4. 遵循现有命名习惯。
5. 不引入新的 UI 框架。
6. 不引入第二套状态管理。
7. 不引入第二套 request。
8. 不大范围重构无关代码。
9. 不修改原商城核心业务。
10. 不生成大量重复工具函数。
11. 状态判断尽量集中。
12. 添加必要注释，但不要写无意义注释。
13. 保持 JavaScript 或 TypeScript 风格与当前仓库一致。
14. 不创建后端尚不存在的接口。
15. 不写假数据和 Mock。

# 十八、构建验证

修改完成后，先查看 `package.json`，执行项目真实存在的微信小程序构建命令。

例如可能是：

```bash
pnpm build:mp-weixin
```

但不能直接假设，必须使用 `package.json` 中真实存在的脚本。

构建失败时：

1. 先修复本次修改造成的错误。
2. 不修改与本阶段无关的大量代码。
3. 如果是仓库原有错误，明确指出原始错误位置。
4. 不得谎称构建成功。

构建完成后执行：

```bash
git status --short
git diff --stat
git diff -- pages/index/user.vue
```

并检查本阶段涉及的关键文件 diff。

# 十九、最终输出格式

完成后必须按以下结构输出：

## 1. 仓库检查结果

* 当前分支
* 初始 `git status`
* 已存在的第一阶段文件
* 未提交代码情况
* 实际构建脚本

## 2. 新增和修改文件

逐个列出：

```text
文件路径
新增或修改
主要作用
```

## 3. 实际对接接口

列出：

```text
请求方法
实际路径
调用位置
用途
```

明确说明是否自动拼接 `/app-api`。

## 4. 后端字段差异

列出候选字段与真实字段的差异。

无法核验的字段必须明确标记，禁止猜测。

## 5. 身份状态流转

完整说明：

```text
未登录
未申请
审核中
已驳回
已通过
已停用
已拉黑
接口失败
```

分别进入什么页面、显示什么操作。

## 6. “我的”页面入口实现

说明：

* 插入位置
* 是否在 DIY 循环之后
* 是否仍在 `s-layout` 内
* 点击后的身份判断流程
* 是否影响商城 DIY 和动态底部导航

## 7. 申请表单字段和校验

列出：

* 实际字段
* 必填项
* 校验规则
* 上传字段格式
* 重新申请回填逻辑

## 8. 模式切换方式

说明：

* `delta_app_mode` 保存内容
* 进入打手模式条件
* 返回商城模式方式
* 为什么它不是权限依据

## 9. 构建结果

提供：

```text
执行命令
是否成功
警告
错误
```

## 10. git diff --stat

原样输出最终统计结果。

## 11. 未解决问题

只列真实存在的问题，例如：

* 后端接口尚不存在
* 字段无法核验
* 后端枚举不明确
* 当前仓库原有构建错误

## 12. 下一阶段建议

只给下一阶段建议，不执行。

下一阶段可以建议：

```text
接单大厅
订单池
任务详情
任务状态流转
完成凭证
收入结算
```

但本次禁止实现这些功能。

# 二十、执行原则

现在开始直接检查仓库并修改代码。

不要先输出长篇设计方案。

不要重复创建第一阶段已有文件。

不要修改 Java 后端。

不要修改 Vue 管理后台。

不要写 Mock。

不要编造接口或字段。

不要执行破坏性 Git 命令。

第二阶段完成并验证后立即停止。
